---
phase: 15-evidence-pipeline-validate-advance-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - minilegion/cli/commands.py
  - tests/test_cli_brief_research.py
  - tests/test_cli_design.py
  - tests/test_cli_plan.py
  - tests/test_cli_execute.py
  - tests/test_cli_review.py
autonomous: true
requirements:
  - VAD-01
must_haves:
  truths:
    - "Stage-producing commands create their artifacts but do not auto-advance `STATE.json.current_stage`."
    - "Forward progression after stage command success happens only when users run `minilegion advance`."
  artifacts:
    - path: "minilegion/cli/commands.py"
      provides: "Removal of forward auto-transition mutations from stage-producing command success paths."
      contains: "brief|research|design|plan|execute|review"
    - path: "tests/test_cli_brief_research.py"
      provides: "Regression checks that `brief`/`research` preserve stage after success."
    - path: "tests/test_cli_design.py"
      provides: "Regression checks that `design` preserves stage after success."
    - path: "tests/test_cli_plan.py"
      provides: "Regression checks that `plan` paths preserve stage after success."
    - path: "tests/test_cli_execute.py"
      provides: "Regression checks that `execute` preserves stage after success."
    - path: "tests/test_cli_review.py"
      provides: "Regression checks that `review` preserve-stage behavior is explicit and non-forward transitions remain intentional."
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "tests/test_cli_brief_research.py"
      via: "brief/research command success assertions"
      pattern: "current_stage"
    - from: "minilegion/cli/commands.py"
      to: "tests/test_cli_design.py|tests/test_cli_plan.py|tests/test_cli_execute.py|tests/test_cli_review.py"
      via: "artifact output stays intact while stage remains unchanged"
      pattern: "current_stage"
---

<objective>
Complete decoupling by removing residual forward auto-advance side effects from stage-producing commands.

Purpose: Ensure command execution and stage progression are explicitly separate so gating remains auditable and predictable.
Output: Stage command implementations without forward auto-transition mutations and regression tests proving preserve-stage semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-RESEARCH.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-VALIDATION.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-02-SUMMARY.md
@minilegion/cli/commands.py
@tests/test_cli_brief_research.py
@tests/test_cli_design.py
@tests/test_cli_plan.py
@tests/test_cli_execute.py
@tests/test_cli_review.py

<interfaces>
From `minilegion/core/state.py`:
```python
FORWARD_TRANSITIONS: dict[Stage, Stage] = {
    Stage.INIT: Stage.BRIEF,
    Stage.BRIEF: Stage.RESEARCH,
    Stage.RESEARCH: Stage.DESIGN,
    Stage.DESIGN: Stage.PLAN,
    Stage.PLAN: Stage.EXECUTE,
    Stage.EXECUTE: Stage.REVIEW,
    Stage.REVIEW: Stage.ARCHIVE,
}
```

From `minilegion/cli/commands.py` (target behavior):
```python
@app.command()
def advance() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing regressions for preserve-stage semantics across stage commands</name>
  <files>tests/test_cli_brief_research.py, tests/test_cli_design.py, tests/test_cli_plan.py, tests/test_cli_execute.py, tests/test_cli_review.py</files>
  <behavior>
    - Test 1: `brief` and `research` still produce expected artifacts and approval outputs while `current_stage` is unchanged.
    - Test 2: `design`, `plan` (fast and normal), `execute`, and `review` success paths keep `current_stage` unchanged.
    - Test 3: tests explicitly document that forward progression occurs only through `advance`.
  </behavior>
  <action>Update stage command test suites to assert "artifact generated + stage preserved" on success paths. Keep any intentional non-forward transitions (for example, review redesign/backtracking behavior) explicit and unchanged. Avoid broad fixture rewrites; only update assertions and setup needed for VAD-04 regressions.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_brief_research.py tests/test_cli_design.py -q</automated>
  </verify>
  <done>RED-phase regressions fail against current auto-advance behavior and clearly define preserve-stage expectations for all stage-producing commands.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: remove forward auto-transition mutations from stage-producing command implementations</name>
  <files>minilegion/cli/commands.py, tests/test_cli_brief_research.py, tests/test_cli_design.py, tests/test_cli_plan.py, tests/test_cli_execute.py, tests/test_cli_review.py</files>
  <behavior>
    - Test 1: pass-path command output and artifact writes remain unchanged.
    - Test 2: no success-path forward mutation (`sm.transition(...)` or `state.current_stage = next_stage`) remains inside stage-producing commands.
    - Test 3: all updated stage command tests pass with preserve-stage assertions.
  </behavior>
  <action>In `minilegion/cli/commands.py`, remove remaining forward transition mutations from `brief`, `research`, `design`, `plan`, `execute`, and `review` success paths while preserving artifact generation, validations, approvals, and user-facing output. Keep explicit non-forward transitions only where they are intentional control flow, then align tests to final command semantics.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_plan.py tests/test_cli_execute.py tests/test_cli_review.py -q</automated>
  </verify>
  <done>Stage-producing commands are artifact-only for success paths, and forward stage movement is exclusively delegated to `advance`.</done>
</task>

</tasks>

<verification>
Run preserve-stage gate coverage:
- `python -m pytest tests/test_cli_brief_research.py tests/test_cli_design.py tests/test_cli_plan.py tests/test_cli_execute.py tests/test_cli_review.py -q`
</verification>

<success_criteria>
- Running any stage-producing command does not advance `STATE.json.current_stage` on success.
- Artifacts and approval outputs from stage commands remain intact.
- Forward stage movement requires explicit `minilegion advance` invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/15-evidence-pipeline-validate-advance-gates/15-03-SUMMARY.md`
</output>
