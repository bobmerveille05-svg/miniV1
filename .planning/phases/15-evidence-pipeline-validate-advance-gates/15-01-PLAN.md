---
phase: 15-evidence-pipeline-validate-advance-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minilegion/core/evidence.py
  - minilegion/cli/commands.py
  - tests/test_evidence.py
  - tests/test_cli_validate_advance.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVD-01
  - EVD-02
  - EVD-03
  - VAD-02
must_haves:
  truths:
    - "Running `minilegion validate <step>` always writes `project-ai/evidence/<step>.validation.json` with canonical evidence fields."
    - "Re-running validate for the same step overwrites that step's evidence file instead of creating extra files."
    - "Validation evidence is machine-readable JSON that downstream commands can parse directly."
    - "`validate` reports pass/fail and does not mutate `STATE.json.current_stage`."
  artifacts:
    - path: "minilegion/core/evidence.py"
      provides: "ValidationEvidence schema plus deterministic read/write helpers per step."
      exports: ["ValidationEvidence", "write_evidence", "read_evidence"]
    - path: "minilegion/cli/commands.py"
      provides: "`validate` command wired to evidence subsystem and stage-safe behavior."
      contains: "def validate"
    - path: "tests/test_evidence.py"
      provides: "Contract coverage for evidence payload fields, overwrite semantics, and read path."
    - path: "tests/test_cli_validate_advance.py"
      provides: "CLI coverage for validate pass/fail exits and no-stage-mutation behavior."
    - path: "tests/test_cli.py"
      provides: "Command registration coverage including `validate`."
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/evidence.py"
      via: "validate command read/write evidence calls"
      pattern: "write_evidence|read_evidence"
    - from: "minilegion/core/evidence.py"
      to: "project-ai/evidence/*.validation.json"
      via: "atomic per-step evidence writes"
      pattern: "write_atomic"
    - from: "minilegion/cli/commands.py"
      to: "STATE.json"
      via: "validate command explicit no-mutation behavior"
      pattern: "current_stage"
---

<objective>
Introduce a first-class evidence pipeline and `validate <step>` command so validation becomes durable, queryable, and decoupled from stage transitions.

Purpose: Establish the machine-readable contract required for gated progression and auditability.
Output: Evidence schema/helpers, validate CLI command, and tests proving contract fields, overwrite semantics, and no stage mutation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-RESEARCH.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-VALIDATION.md
@minilegion/cli/commands.py
@minilegion/core/state.py
@minilegion/core/history.py
@minilegion/core/file_io.py
@tests/test_cli.py

<interfaces>
From `minilegion/core/state.py`:
```python
class Stage(str, Enum):
    INIT = "init"
    BRIEF = "brief"
    RESEARCH = "research"
    DESIGN = "design"
    PLAN = "plan"
    EXECUTE = "execute"
    REVIEW = "review"
    ARCHIVE = "archive"

def load_state(path: Path) -> ProjectState: ...
def save_state(state: ProjectState, path: Path) -> None: ...
```

From `minilegion/core/history.py`:
```python
class HistoryEvent(BaseModel):
    event_type: str
    stage: str
    timestamp: str
    actor: str = "system"
    tool_used: str = "minilegion"
    notes: str = ""
```

From `minilegion/core/file_io.py`:
```python
def write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: create failing evidence and validate command contract tests (wave 0)</name>
  <files>tests/test_evidence.py, tests/test_cli_validate_advance.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: writing evidence for `brief` produces `project-ai/evidence/brief.validation.json` with fields `step`, `status`, `checks_passed`, `validator`, `tool_used`, `date`, `notes`.
    - Test 2: writing evidence for the same step twice overwrites the same file and keeps the latest payload.
    - Test 3: reading evidence returns a typed payload usable by downstream gate logic.
    - Test 4: `minilegion validate <step>` exits 0 on pass and 1 on fail while leaving `current_stage` unchanged.
    - Test 5: root CLI help includes `validate` command registration.
  </behavior>
  <action>Add `tests/test_evidence.py` and `tests/test_cli_validate_advance.py` with deterministic fixtures for pass/fail validation outcomes and overwrite assertions, then update `tests/test_cli.py` command list to include `validate`. Use `CliRunner` + monkeypatch patterns already used in the repo; keep checks stage-scoped and avoid asserting transient timestamps.</action>
  <verify>
    <automated>python -m pytest tests/test_evidence.py tests/test_cli_validate_advance.py::TestValidate tests/test_cli.py::TestCLIHelp::test_all_commands_registered -q</automated>
  </verify>
  <done>RED-phase tests exist for EVD and validate behavior, and they fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement evidence subsystem and wire `validate &lt;step&gt;` command</name>
  <files>minilegion/core/evidence.py, minilegion/cli/commands.py</files>
  <behavior>
    - Test 1: evidence writes are atomic and deterministic (`<step>.validation.json`).
    - Test 2: validate computes step checks, writes evidence for every invocation, and reports pass/fail via exit code.
    - Test 3: validate does not call state transition logic or mutate `current_stage`.
  </behavior>
  <action>Create `minilegion/core/evidence.py` with a `ValidationEvidence` Pydantic model and helpers to resolve file paths, write evidence atomically, and read evidence by step. Add `@app.command()` `validate(step: str)` in `minilegion/cli/commands.py` that validates supported steps (`brief`, `research`, `design`, `plan`, `execute`, `review`) against current project artifacts, writes evidence every run (pass or fail), prints concise status, and exits non-zero on failure without changing stage. Keep unsupported steps explicit with a clear error and exit 1; do not infer readiness from approval flags.</action>
  <verify>
    <automated>python -m pytest tests/test_evidence.py tests/test_cli_validate_advance.py::TestValidate tests/test_cli.py::TestCLIHelp::test_all_commands_registered -q</automated>
  </verify>
  <done>`validate` is available, evidence bundles are durable and machine-readable, and stage remains unchanged after validation.</done>
</task>

</tasks>

<verification>
Run the phase-1 evidence gate:
- `python -m pytest tests/test_evidence.py tests/test_cli_validate_advance.py::TestValidate tests/test_cli.py -q`
</verification>

<success_criteria>
- `project-ai/evidence/` is created on first validation and contains per-step `*.validation.json` files with the required fields.
- Re-validating a step overwrites that step's evidence file with the latest result.
- `minilegion validate <step>` reports pass/fail and never advances the workflow stage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-evidence-pipeline-validate-advance-gates/15-01-SUMMARY.md`
</output>
