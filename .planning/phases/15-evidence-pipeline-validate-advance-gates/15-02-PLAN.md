---
phase: 15-evidence-pipeline-validate-advance-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - minilegion/cli/commands.py
  - minilegion/core/config.py
  - tests/test_config.py
  - tests/test_cli_validate_advance.py
  - tests/test_cli.py
autonomous: true
requirements:
  - VAD-04
  - VAD-03
  - CFG-07
must_haves:
  truths:
    - "`minilegion advance` is the only forward stage mutator and succeeds only when current-step evidence is passing."
    - "`minilegion advance` exits non-zero with clear gate messaging when evidence is missing or failing."
    - "Configs that omit `workflow` still load successfully with `strict_mode=true` and `require_validation=true` defaults."
  artifacts:
    - path: "minilegion/core/config.py"
      provides: "WorkflowConfig schema with backward-compatible defaults for strict/validation gates."
      contains: "class WorkflowConfig"
    - path: "minilegion/cli/commands.py"
      provides: "`advance` command with evidence-gated forward stage transition behavior."
      contains: "def advance"
    - path: "tests/test_cli_validate_advance.py"
      provides: "Advance gate behavior coverage for pass, missing evidence, and failing evidence paths."
    - path: "tests/test_config.py"
      provides: "CFG-07 coverage for defaults and partial workflow overrides."
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/evidence.py"
      via: "advance gate reads current-stage evidence"
      pattern: "read_evidence"
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/state.py"
      via: "single forward transition path in advance"
      pattern: "FORWARD_TRANSITIONS|StateMachine"
    - from: "minilegion/core/config.py"
      to: "minilegion/cli/commands.py"
      via: "workflow.strict_mode and workflow.require_validation enforcement"
      pattern: "workflow"
---

<objective>
Implement the explicit `advance` evidence gate and restore backward-compatible workflow strictness defaults.

Purpose: Close the validate->advance control loop so progression is explicit, auditable, and backward compatible with existing configs.
Output: `advance` command behavior, workflow config extensions, and focused tests for gate and defaults.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-RESEARCH.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-VALIDATION.md
@.planning/phases/15-evidence-pipeline-validate-advance-gates/15-01-SUMMARY.md
@minilegion/cli/commands.py
@minilegion/core/config.py
@minilegion/core/state.py
@minilegion/core/history.py
@tests/test_config.py

<interfaces>
From `minilegion/core/state.py`:
```python
FORWARD_TRANSITIONS: dict[Stage, Stage] = {
    Stage.INIT: Stage.BRIEF,
    Stage.BRIEF: Stage.RESEARCH,
    Stage.RESEARCH: Stage.DESIGN,
    Stage.DESIGN: Stage.PLAN,
    Stage.PLAN: Stage.EXECUTE,
    Stage.EXECUTE: Stage.REVIEW,
    Stage.REVIEW: Stage.ARCHIVE,
}

class StateMachine:
    def can_transition(self, target: Stage) -> bool: ...
    def transition(self, target: Stage) -> None: ...
```

From `minilegion/core/config.py`:
```python
class MiniLegionConfig(BaseModel):
    context: ContextConfig = Field(default_factory=ContextConfig)
```

From `minilegion/core/history.py`:
```python
def append_event(project_dir: Path, event: HistoryEvent) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing tests for advance gate and workflow config defaults</name>
  <files>tests/test_cli_validate_advance.py, tests/test_config.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: `advance` succeeds only when current-stage evidence status is `pass`.
    - Test 2: `advance` fails with exit 1 and clear message when evidence is missing/failing.
    - Test 3: successful `advance` updates `current_stage` by one step and appends history event.
    - Test 4: `MiniLegionConfig` accepts missing `workflow` block and defaults `strict_mode=true`, `require_validation=true`.
    - Test 5: CLI help includes `advance` command.
  </behavior>
  <action>Expand test coverage before implementation: add `TestAdvancePass` and `TestAdvanceReject` in `tests/test_cli_validate_advance.py`, add `TestWorkflowConfig` in `tests/test_config.py`, and add `advance` to command registration assertions in `tests/test_cli.py`. Keep this plan scoped to explicit advance gating and config compatibility; stage-command no-auto-advance regressions are handled in Plan 15-03.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_validate_advance.py::TestAdvancePass tests/test_cli_validate_advance.py::TestAdvanceReject tests/test_config.py::TestWorkflowConfig tests/test_cli.py::TestCLIHelp::test_all_commands_registered -q</automated>
  </verify>
  <done>All VAD-04/VAD-03/CFG-07 expectations are explicitly covered and failing prior to command/config changes.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement workflow config defaults and `advance` command evidence gate</name>
  <files>minilegion/core/config.py, minilegion/cli/commands.py</files>
  <behavior>
    - Test 1: old configs without `workflow` load with strict defaults.
    - Test 2: `advance` checks evidence for `state.current_stage` and transitions only on passing status.
    - Test 3: missing/failing evidence path returns clear non-zero refusal.
    - Test 4: successful advance appends a history event describing old/new stage transition.
  </behavior>
  <action>Add `WorkflowConfig` to `minilegion/core/config.py` with `strict_mode: bool = True` and `require_validation: bool = True`, then include `workflow: WorkflowConfig = Field(default_factory=WorkflowConfig)` in `MiniLegionConfig` for non-breaking defaults. In `minilegion/cli/commands.py`, add `@app.command()` `advance()` that loads state/config, resolves next stage via `FORWARD_TRANSITIONS`, gates on current-step evidence (`read_evidence`) when `workflow.require_validation` is true, exits 1 with clear "validation required" messaging when gate fails, and on success performs one-step transition plus `append_event(...)` and `save_state(...)`.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_validate_advance.py::TestAdvancePass tests/test_cli_validate_advance.py::TestAdvanceReject tests/test_config.py::TestWorkflowConfig -q</automated>
  </verify>
  <done>`advance` is the enforced gatekeeper for forward transitions and CFG-07 defaults are backward compatible.</done>
</task>

</tasks>

<verification>
Run phase gate coverage:
- `python -m pytest tests/test_evidence.py tests/test_cli_validate_advance.py tests/test_config.py tests/test_cli.py -q`
</verification>

<success_criteria>
- `minilegion advance` advances exactly one stage only when current-step evidence is passing.
- Missing/failing evidence blocks advance with non-zero exit and explicit guidance.
- `workflow.strict_mode` and `workflow.require_validation` are accepted config fields with defaults true and no breakage for existing configs.
</success_criteria>

<output>
After completion, create `.planning/phases/15-evidence-pipeline-validate-advance-gates/15-02-SUMMARY.md`
</output>
