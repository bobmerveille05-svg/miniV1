# 08-01-PLAN.md: Plan Stage Implementation

## Goal

Replace the `plan()` stub in `commands.py` with a full implementation that produces PLAN.json + PLAN.md via the Planner LLM role, with approval gate and state transition.

## Tasks

### Task A: Wire plan() command

**File**: `minilegion/cli/commands.py`

**Changes**:
1. Add `approve_plan` to the approval import block (line 15-20)
2. Replace `_pipeline_stub(Stage.PLAN, ...)` body in `plan()` with full implementation

**Pattern** (identical to design() at lines 333-394):
```
find_project_dir() → load_config(project_dir.parent) → load_state → StateMachine →
can_transition(Stage.PLAN) → check_preflight(Stage.PLAN, project_dir) →
read BRIEF.md, RESEARCH.json, DESIGN.json →
load_prompt("planner") + render_prompt(project_name, brief_content, research_json, design_json) →
OpenAIAdapter(config) → validate_with_retry(llm_call, user_message, "plan", config, project_dir) →
save_dual(plan_data, PLAN.json, PLAN.md) →
approve_plan(state, STATE.json, plan_md) →
sm.transition(Stage.PLAN) + state.current_stage = Stage.PLAN.value + save_state()
```

**ApprovalError caught BEFORE MiniLegionError** (established pattern)

**Preserve** `--fast` and `--skip-research-design` flags in function signature (already declared, used in future Phase 12)

### Task B: Create test_cli_plan.py

**File**: `tests/test_cli_plan.py`

**Pattern**: Mirror TestDesignCommand (test_cli_design.py) with:
- Fixture: `_write_design_state()` — STATE.json at `current_stage=design`, approvals include `design_approved=True`; plus BRIEF.md, RESEARCH.json, DESIGN.json
- Valid plan fixture: `VALID_PLAN` dict matching PlanSchema fields

**10 Tests**:
1. `test_plan_calls_preflight` — check_preflight called with Stage.PLAN
2. `test_plan_calls_llm` — validate_with_retry called with artifact="plan"
3. `test_plan_saves_dual_output` — PLAN.json and PLAN.md exist after successful run
4. `test_plan_preflight_failure_exits_1` — PreflightError → exit code 1
5. `test_plan_llm_error_exits_1` — LLMError → exit code 1
6. `test_plan_writes_atomically_before_approval` — PLAN.json + PLAN.md exist even on rejection
7. `test_plan_approval_accepted_transitions_state` — plan_approved=True in STATE.json after approval
8. `test_plan_state_current_stage_is_plan_after_approval` — current_stage="plan" after approval
9. `test_plan_rejection_exits_0` — rejection exits 0, "rejected" in output
10. `test_plan_rejection_leaves_state_unchanged` — current_stage="design" after rejection

## Acceptance Criteria

- All 10 new tests pass
- All 441 existing tests still pass (451 total)
- `minilegion plan` from design stage produces PLAN.json + PLAN.md
- Rejection leaves STATE.json with current_stage=design
