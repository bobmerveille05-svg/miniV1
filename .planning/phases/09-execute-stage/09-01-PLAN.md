# 09-01-PLAN.md: Patcher Module

## Goal

Create `minilegion/core/patcher.py` implementing `apply_patch()` for filesystem patch application.

## Tasks

### Task A: Create patcher.py

**File**: `minilegion/core/patcher.py`

```python
"""Patch application module for MiniLegion.

Applies ChangedFile patches to the filesystem (BUILD-04).
"""

from pathlib import Path
from minilegion.core.file_io import write_atomic
from minilegion.core.schemas import ChangedFile


def apply_patch(changed_file: ChangedFile, project_root: Path, dry_run: bool = False) -> str:
    """Apply a single file patch to the filesystem.

    Args:
        changed_file: The patch to apply (path, action, content).
        project_root: Root directory for resolving relative paths.
        dry_run: If True, return description without modifying files.

    Returns:
        Human-readable description of the change (for display/approval).
    """
    target = Path(project_root) / changed_file.path
    
    if changed_file.action == "delete":
        description = f"DELETE {changed_file.path}"
        if not dry_run:
            if target.exists():
                target.unlink()
        return description
    
    # create or modify
    action_label = changed_file.action.upper()
    line_count = len(changed_file.content.splitlines())
    description = f"{action_label} {changed_file.path} ({line_count} lines)"
    
    if not dry_run:
        target.parent.mkdir(parents=True, exist_ok=True)
        write_atomic(target, changed_file.content)
    
    return description
```

### Task B: Create tests/test_patcher.py

**8 tests:**
1. `test_apply_patch_create_writes_file` — create action writes file with content
2. `test_apply_patch_modify_overwrites_file` — modify action overwrites existing file
3. `test_apply_patch_delete_removes_file` — delete action removes existing file
4. `test_apply_patch_delete_missing_file_noop` — delete on missing file does not raise
5. `test_apply_patch_dry_run_create_no_file_written` — dry_run=True: file not created
6. `test_apply_patch_dry_run_delete_no_file_removed` — dry_run=True: file not deleted
7. `test_apply_patch_returns_description` — returns human-readable string
8. `test_apply_patch_creates_parent_dirs` — creates parent directories if needed

## Acceptance Criteria

- 8 new tests pass
- Existing 451 tests still pass (459 total)
