---
phase: 01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minilegion/core/config.py
  - minilegion/cli/config_commands.py
  - tests/test_config.py
  - tests/test_config_commands.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Config supports both primary and small model settings with safe defaults."
    - "Tool permissions default to confirmation mode unless user explicitly changes it."
    - "Config UX clearly supports curated recommended models and full model catalog selection."
    - "Model aliases resolve to canonical model IDs during config flows."
  artifacts:
    - path: "minilegion/core/config.py"
      provides: "Hardened config schema with new fields and validation/default behavior"
    - path: "minilegion/cli/config_commands.py"
      provides: "Recommended/all model selection and alias resolution in config commands"
    - path: "tests/test_config.py"
      provides: "Automated coverage for new config fields/defaults"
    - path: "tests/test_config_commands.py"
      provides: "Automated coverage for catalog and alias CLI behavior"
  key_links:
    - from: "minilegion/cli/config_commands.py"
      to: "minilegion/core/config.py"
      via: "load_config + model_copy + model_dump_json"
      pattern: "load_config|model_copy|model_dump_json"
    - from: "tests/test_config_commands.py"
      to: "minilegion/cli/config_commands.py"
      via: "CLI runner exercises config init/model flows"
      pattern: "runner\.invoke\(app, \[\"config\""
---

<objective>
Implement the configuration hardening layer so new model-tier, permissions, catalog, and alias behaviors are first-class and backward compatible.

Purpose: Make provider/model selection safer and less error-prone before runtime stages.
Output: Hardened `MiniLegionConfig` contract and CLI config command behavior with regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-RESEARCH.md
@minilegion/core/config.py
@minilegion/cli/config_commands.py
@tests/test_config.py
@tests/test_config_commands.py

<interfaces>
From minilegion/core/config.py:
```python
class MiniLegionConfig(BaseModel):
    provider: str = "openai"
    model: str = "gpt-4o"
    api_key_env: str = "OPENAI_API_KEY"
    base_url: str | None = None
    timeout: int = 120
    max_retries: int = 2
    engines: dict[str, str] = Field(default_factory=dict)
```

From minilegion/cli/config_commands.py:
```python
RECOMMENDED_MODELS: dict[str, list[tuple[str, str]]] = {...}

@config_app.command("init")
def config_init() -> None: ...

@config_app.command("model")
def config_model() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: extend config schema with hardened fields and defaults</name>
  <files>minilegion/core/config.py, tests/test_config.py</files>
  <behavior>
    - Test 1: `MiniLegionConfig()` exposes defaults for `small_model`, `tool_permissions`, `recommended_models`, `all_models`, `model_aliases`, `context_auto_compact`, and `provider_healthcheck`.
    - Test 2: Partial/legacy JSON config still loads with defaults for all new keys.
    - Test 3: Invalid `tool_permissions` value raises config validation error path through `load_config()`.
  </behavior>
  <action>Add the new hardening fields to `MiniLegionConfig` with backward-compatible defaults, keeping existing load behavior and exception wrapping. Implement explicit validation for `tool_permissions` with default `confirm` per user decision, and preserve existing fields/behavior so old config files continue to parse unchanged.</action>
  <verify>
    <automated>python -m pytest tests/test_config.py -q</automated>
  </verify>
  <done>Config model accepts old and new config files, enforces tool permission semantics, and test coverage proves default/fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement recommended-vs-all model catalog and alias resolution in CLI config UX</name>
  <files>minilegion/cli/config_commands.py, tests/test_config_commands.py</files>
  <behavior>
    - Test 1: Config commands can display/select from curated recommended models and extended all-model lists.
    - Test 2: `model_aliases` entries resolve to canonical model IDs before writing config.
    - Test 3: Existing provider/model commands remain backward compatible when new keys are absent in legacy config files.
  </behavior>
  <action>Add `ALL_MODELS` provider catalogs and deterministic selection logic so users can choose recommended or full lists without breaking existing command flow. Add alias resolution utility that maps alias input through `model_aliases` to canonical IDs, then persists canonical IDs in config. Keep command UX interactive and fail with clear messages for unknown aliases or missing catalogs.</action>
  <verify>
    <automated>python -m pytest tests/test_config_commands.py -q</automated>
  </verify>
  <done>`config init` and `config model` support recommended/all catalog behavior and alias resolution with tests covering happy-path and invalid input handling.</done>
</task>

</tasks>

<verification>
Run targeted config suite after each task and keep full config-related suite green:
- `python -m pytest tests/test_config.py tests/test_config_commands.py -q`
</verification>

<success_criteria>
- New hardening config keys exist with safe defaults and legacy compatibility.
- `tool_permissions` default is confirmation-safe and validated.
- Recommended vs all model selection is explicit and test-covered.
- Alias inputs resolve deterministically to model IDs in written config.
</success_criteria>

<output>
After completion, create `.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-01-SUMMARY.md`
</output>
