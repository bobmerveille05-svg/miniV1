---
phase: 01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minilegion/core/config.py
  - minilegion/cli/config_commands.py
  - tests/test_config.py
  - tests/test_config_commands.py
  - README.md
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Users can configure both primary and small-model defaults without breaking existing config files."
    - "Tool permissions default to confirm and reject unsupported values at config-load time."
    - "Config CLI reads the configured recommended/full model catalogs and clearly separates the recommended path from the full catalog for a provider."
    - "Alias input resolves to canonical model IDs before config is written back to disk."
  artifacts:
    - path: "minilegion/core/config.py"
      provides: "Validated config schema with hardening fields and backward-compatible defaults"
    - path: "minilegion/cli/config_commands.py"
      provides: "Interactive provider/model selection helpers that read configured recommended/all catalogs and apply alias resolution"
    - path: "tests/test_config.py"
      provides: "Regression coverage for new config defaults, validation, and legacy-file loading"
    - path: "tests/test_config_commands.py"
      provides: "CLI coverage for catalog branching and alias persistence"
    - path: "README.md"
      provides: "User-facing config contract updated to match shipped CLI and config fields"
  key_links:
    - from: "minilegion/cli/config_commands.py"
      to: "minilegion/core/config.py"
      via: "load_config(), model_copy(update=...), model_dump_json()"
      pattern: "load_config|model_copy|model_dump_json"
    - from: "tests/test_config_commands.py"
      to: "minilegion/cli/config_commands.py"
      via: "Typer CliRunner config init/model flows"
      pattern: "runner\.invoke\(app, \[\"config\""
---

<objective>
Harden the config contract and interactive config UX so provider/model setup is explicit, validated, and canonical before runtime stages use it.

Purpose: Make misconfiguration less likely and keep legacy `minilegion.config.json` files loading cleanly.
Output: Extended `MiniLegionConfig`, upgraded config CLI flows, and docs/tests that describe the new config behavior accurately.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-RESEARCH.md
@minilegion/core/config.py
@minilegion/cli/config_commands.py
@tests/test_config.py
@tests/test_config_commands.py
@README.md

<interfaces>
From minilegion/core/config.py:
```python
class MiniLegionConfig(BaseModel):
    provider: str = "openai"
    model: str = "gpt-4o"
    api_key_env: str = "OPENAI_API_KEY"
    base_url: str | None = None
    timeout: int = 120
    max_retries: int = 2
    engines: dict[str, str] = Field(default_factory=dict)
    scan_max_depth: int = 5
    scan_max_files: int = 200
    scan_max_file_size_kb: int = 100

def load_config(project_dir: Path) -> MiniLegionConfig: ...
```

From minilegion/cli/config_commands.py:
```python
PROVIDERS: dict[str, str] = {...}
DEFAULT_ENV_VAR: dict[str, str] = {...}
RECOMMENDED_MODELS: dict[str, list[tuple[str, str]]] = {...}

def _prompt_choice(prompt: str, options: list[str]) -> int: ...

@config_app.command("init")
def config_init() -> None: ...

@config_app.command("model")
def config_model() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: extend config schema with hardened fields and safe defaults</name>
  <files>minilegion/core/config.py, tests/test_config.py</files>
  <behavior>
    - Test 1: `MiniLegionConfig()` exposes defaults for `small_model`, `tool_permissions`, `recommended_models`, `all_models`, `model_aliases`, `context_auto_compact`, and `provider_healthcheck`.
    - Test 2: Legacy or partial JSON config still loads and backfills every new field with defaults.
    - Test 3: Invalid `tool_permissions` values fail through `load_config()` with the normal `ConfigError` path.
    - Test 4: `recommended_models`, `all_models`, and `model_aliases` load as provider-keyed runtime catalogs/aliases the CLI can consume without hardcoded fallback shape mismatches.
  </behavior>
  <action>Add the new hardening fields directly to `MiniLegionConfig` using typed defaults and narrow validation. `tool_permissions` must default to `confirm` per locked decision, and `small_model` must be a first-class field with safe fallback semantics rather than a derived alias. Treat `recommended_models`, `all_models`, and `model_aliases` as real config-backed runtime inputs by defining stable provider-keyed shapes that the CLI can read directly instead of duplicating separate hardcoded catalogs. Keep `load_config()` backward compatible with existing files and avoid any config format migration.</action>
  <verify>
    <automated>python -m pytest tests/test_config.py -q</automated>
  </verify>
  <done>Config loading accepts old and new files, all new keys are present with deterministic defaults, and invalid permission values are rejected immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add recommended-vs-all catalog helpers and canonical alias resolution</name>
  <files>minilegion/cli/config_commands.py, tests/test_config_commands.py</files>
  <behavior>
    - Test 1: `config init` and `config model` read `recommended_models` and `all_models` from loaded config, offer recommended models as the default path, and allow switching to the broader full catalog.
    - Test 2: Alias input from `model_aliases` resolves to the canonical provider model ID before persistence.
    - Test 3: Unknown alias or missing configured catalog entries fail with clear CLI feedback instead of silently writing bad config.
  </behavior>
  <action>Refactor `config init` and `config model` so both commands load `recommended_models`, `all_models`, and `model_aliases` from `MiniLegionConfig` and use one shared branching helper for recommended-vs-all selection. Resolve user input through `model_aliases`, then validate against the chosen configured catalog before writing canonical model IDs back to disk. Keep the catalogs deterministic and in-repo via config defaults; do not fetch live provider catalogs or bypass the config schema with separate CLI-only constants.</action>
  <verify>
    <automated>python -m pytest tests/test_config_commands.py -q</automated>
  </verify>
  <done>Both config commands support curated and expanded model selection, aliases persist as canonical IDs, and invalid inputs produce tested failure messaging.</done>
</task>

<task type="auto">
  <name>task 3: update user-facing config documentation to match shipped behavior</name>
  <files>README.md</files>
  <action>Update the configuration section so it documents the new fields (`small_model`, `tool_permissions`, `recommended_models`, `all_models`, `model_aliases`, `context_auto_compact`, `provider_healthcheck`) and the runtime behaviors attached to them. Be explicit that the config CLI reads `recommended_models` and `all_models` from config defaults/overrides to build the selection flow, and that aliases from `model_aliases` resolve before persistence. Keep docs aligned with the actual default values and do not promise dynamic catalog fetching or healthchecks outside research.</action>
  <verify>
    <automated>python -m pytest tests/test_config.py tests/test_config_commands.py -q</automated>
  </verify>
  <done>`README.md` accurately reflects the config schema and CLI behavior implemented in this plan, preventing doc drift for Phase 1 UX.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_config.py tests/test_config_commands.py -q`
- Confirm `README.md` examples and field list match the actual serialized `MiniLegionConfig` defaults.
</verification>

<success_criteria>
- `small_model` and `tool_permissions` are first-class validated config fields with backward-compatible defaults.
- Users can intentionally choose recommended or full provider model catalogs sourced from config.
- Alias input never persists non-canonical model IDs.
- Docs describe the same config contract the code enforces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-01-SUMMARY.md`
</output>
