---
phase: 01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - minilegion/core/provider_health.py
  - minilegion/cli/commands.py
  - minilegion/core/pipeline.py
  - tests/test_provider_health.py
  - tests/test_cli_brief_research.py
autonomous: true
requirements: [CFG-05, CFG-06]
must_haves:
  truths:
    - "Research reads `provider_healthcheck` from config and refuses to start when the check is enabled and the configured provider is not ready."
    - "When `provider_healthcheck` is enabled, the healthcheck runs before preflight, scanner work, prompt rendering, or LLM calls."
    - "Oversized scanned context is compacted deterministically when config enables auto-compaction."
  artifacts:
    - path: "minilegion/core/provider_health.py"
      provides: "Reusable provider-readiness checks with actionable failure messages"
    - path: "minilegion/cli/commands.py"
      provides: "Research-stage wiring for config-driven healthcheck ordering and context compaction"
    - path: "minilegion/core/pipeline.py"
      provides: "Service-layer parity for config-driven research gating and compaction behavior"
    - path: "tests/test_provider_health.py"
      provides: "Unit coverage for provider health pass/fail branches"
    - path: "tests/test_cli_brief_research.py"
      provides: "Research command ordering and compaction regression tests"
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/provider_health.py"
      via: "research() calls run_provider_healthcheck(config) immediately after load_config()"
      pattern: "load_config\(project_dir\.parent\).*run_provider_healthcheck"
    - from: "minilegion/cli/commands.py"
      to: "minilegion.core.context_scanner.scan_codebase"
      via: "scanner output is compacted before render_prompt() when enabled"
      pattern: "scan_codebase|context_auto_compact|render_prompt"
    - from: "minilegion/core/pipeline.py"
      to: "minilegion/core/provider_health.py"
      via: "run_research() invokes run_provider_healthcheck(config) before preflight or adapter work"
      pattern: "run_provider_healthcheck\(config\)"
    - from: "minilegion/core/pipeline.py"
      to: "minilegion/core/context_scanner.py"
      via: "run_research() normalizes scan_max_file_size_kb usage and compacts scanned context before render_prompt()"
      pattern: "scan_max_file_size_kb|context_auto_compact|render_prompt"
---

<objective>
Add the pre-research fail-fast safety gate and deterministic context compaction so research only runs when the provider is usable and the prompt budget stays bounded.

Purpose: Stop wasted scanner/LLM work early and make research prompt size control explicit in config.
Output: A reusable provider-health module plus research orchestration updates with focused regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-RESEARCH.md
@minilegion/cli/commands.py
@minilegion/core/pipeline.py
@minilegion/core/context_scanner.py
@minilegion/adapters/factory.py
@minilegion/adapters/openai_adapter.py
@minilegion/adapters/openai_compatible_adapter.py
@minilegion/adapters/ollama_adapter.py
@minilegion/adapters/anthropic_adapter.py
@minilegion/adapters/gemini_adapter.py
@tests/test_cli_brief_research.py
@tests/test_adapters.py

<interfaces>
From minilegion/cli/commands.py:
```python
@app.command()
def research() -> None:
    project_dir = find_project_dir()
    state = load_state(project_dir / "STATE.json")
    sm = StateMachine(Stage(state.current_stage), state.approvals)
    config = load_config(project_dir.parent)
    check_preflight(Stage.RESEARCH, project_dir)
    codebase_context = scan_codebase(project_dir, config)
    user_message = render_prompt(..., codebase_context=codebase_context)
```

From minilegion/core/pipeline.py:
```python
def run_research(
    project_dir: Path,
    state: ProjectState,
    sm: StateMachine,
    config: MiniLegionConfig,
) -> None: ...
```

From minilegion/adapters/factory.py:
```python
def get_adapter(config: MiniLegionConfig) -> LLMAdapter: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: create reusable provider healthcheck coverage and helper</name>
  <files>minilegion/core/provider_health.py, tests/test_provider_health.py</files>
  <behavior>
    - Test 1: Supported providers with the required local prerequisites pass quickly when `provider_healthcheck` is enabled, without invoking research work.
    - Test 2: Missing env vars or required base URL data fail with actionable messages for remote providers.
    - Test 3: Ollama healthcheck performs only a lightweight local readiness probe and fails clearly when the endpoint is unavailable.
  </behavior>
  <action>Create `run_provider_healthcheck(config)` as a deterministic core helper and pair it with a tiny config gate so callers can explicitly honor `config.provider_healthcheck`. When enabled, validate provider support and required local prerequisites first, then perform only the minimal extra probe needed for provider readiness; per research recommendations, keep remote providers to local env/config checks and use a lightweight local HTTP probe only for Ollama. Raise existing MiniLegion error types with remediation text, and avoid putting this logic inside adapter call paths where it would happen too late.</action>
  <verify>
    <automated>python -m pytest tests/test_provider_health.py -q</automated>
  </verify>
  <done>Provider health rules are encoded in one reusable module, and fast unit tests prove deterministic pass/fail behavior across supported providers.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire healthcheck-before-research and deterministic context compaction</name>
  <files>minilegion/cli/commands.py, minilegion/core/pipeline.py, tests/test_cli_brief_research.py</files>
  <behavior>
    - Test 1: `research()` reads `config.provider_healthcheck`; when true it runs provider healthcheck immediately after config load and before preflight, scanner, prompt rendering, or `validate_with_retry()`.
    - Test 2: When `config.provider_healthcheck` is false, research skips the readiness probe and continues through the normal flow without calling the healthcheck helper.
    - Test 3: Failed healthcheck exits with code 1 and proves scanner and LLM code paths were not called.
    - Test 4: When `context_auto_compact` is enabled and scanned context exceeds the threshold, the rendered prompt receives deterministic compacted text with an explicit truncation marker.
    - Test 5: The research flow uses a single normalized file-size contract so `scan_max_file_size` legacy references cannot break compaction or prompt-budget logic.
  </behavior>
  <action>Update both CLI orchestration and `run_research()` service-layer logic so they explicitly read and honor `config.provider_healthcheck` the same way: when true, run the fail-fast readiness check immediately after config load; when false, skip that probe and continue normally. Add a deterministic compaction helper that runs after `scan_codebase()` and before `render_prompt()` when `config.context_auto_compact` is true; preserve scanner output order, use a fixed character threshold, and append an explicit compaction marker rather than heuristic summarization. While touching that prompt-budget path, normalize the known `scan_max_file_size` vs `scan_max_file_size_kb` mismatch to one contract or add an explicit compatibility shim so both CLI and service-layer code read the config safely without attribute errors. Add focused regressions in `tests/test_cli_brief_research.py` for both the `provider_healthcheck` on/off behavior and the old field-name reference. Do not move compaction into scanner internals and do not expand healthchecks beyond the research stage.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_brief_research.py -k "healthcheck or compact or scan_max_file_size" -q</automated>
  </verify>
  <done>Research fails fast on provider-readiness issues, never performs downstream work on failure, and compacts oversized context in a predictable, test-covered way.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_provider_health.py -q`
- `python -m pytest tests/test_cli_brief_research.py -k "healthcheck or compact or scan_max_file_size" -q`
- `python -m pytest tests/test_config.py tests/test_config_commands.py tests/test_provider_health.py tests/test_cli_brief_research.py -q`
</verification>

<success_criteria>
- `provider_healthcheck` is explicitly honored by research orchestration: enabled runs before any research-stage scanner or LLM work, disabled skips the probe cleanly.
- Healthcheck failures are actionable and short-circuit the research command.
- `context_auto_compact` changes prompt size deterministically without changing scanner internals.
- CLI and service-layer research orchestration stay behaviorally aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-02-SUMMARY.md`
</output>
