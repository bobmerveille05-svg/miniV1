---
phase: 01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - minilegion/core/provider_health.py
  - minilegion/cli/commands.py
  - tests/test_cli_brief_research.py
  - tests/test_provider_health.py
autonomous: true
requirements: [CFG-05, CFG-06]
must_haves:
  truths:
    - "Research command performs provider readiness checks before scanner and LLM work."
    - "Research exits early with actionable diagnostics when provider healthcheck fails."
    - "Large research context is automatically compacted when enabled in config."
  artifacts:
    - path: "minilegion/core/provider_health.py"
      provides: "Provider healthcheck utility callable from CLI stages"
    - path: "minilegion/cli/commands.py"
      provides: "Research-stage integration for healthcheck and context auto-compact"
    - path: "tests/test_cli_brief_research.py"
      provides: "Integration coverage for pre-research healthcheck and compact behavior"
    - path: "tests/test_provider_health.py"
      provides: "Fast unit tests for provider health decision paths"
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/provider_health.py"
      via: "research() invokes healthcheck before preflight/scanner"
      pattern: "research\(|provider_health"
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/context_scanner.py"
      via: "scan output compacted before prompt render when enabled"
      pattern: "scan_codebase|context_auto_compact"
---

<objective>
Add pre-research provider health enforcement and context auto-compact behavior so research fails fast and stays within safe prompt budgets.

Purpose: Prevent wasted work/token usage when provider configuration is not runnable.
Output: Reusable provider healthcheck module wired into `research()` with automated validation coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-RESEARCH.md
@minilegion/cli/commands.py
@minilegion/core/context_scanner.py
@minilegion/adapters/factory.py
@tests/test_cli_brief_research.py

<interfaces>
From minilegion/cli/commands.py:
```python
@app.command()
def research() -> None:
    config = load_config(project_dir.parent)
    check_preflight(Stage.RESEARCH, project_dir)
    codebase_context = scan_codebase(project_dir, config)
```

From minilegion/adapters/factory.py:
```python
def get_adapter(config: MiniLegionConfig) -> LLMAdapter: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add provider healthcheck utility and tests</name>
  <files>minilegion/core/provider_health.py, tests/test_provider_health.py</files>
  <behavior>
    - Test 1: Healthcheck passes for valid provider configs and required credential shape.
    - Test 2: Healthcheck fails with clear error when required API key env var is missing for remote providers.
    - Test 3: Unsupported provider or invalid config path returns deterministic failure message.
  </behavior>
  <action>Create `provider_health.py` with a reusable `run_provider_healthcheck(config)` function that performs lightweight, deterministic readiness checks (provider support + credential/env availability and provider-specific preconditions). Keep checks fast and local-first; provide actionable remediation text in raised errors.</action>
  <verify>
    <automated>python -m pytest tests/test_provider_health.py -q</automated>
  </verify>
  <done>Healthcheck utility exists, is imported cleanly by CLI code, and unit tests prove pass/fail decision behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire healthcheck and context auto-compact into research command</name>
  <files>minilegion/cli/commands.py, tests/test_cli_brief_research.py</files>
  <behavior>
    - Test 1: `research()` invokes provider healthcheck immediately after config load and before scanner/LLM actions.
    - Test 2: Failed healthcheck exits with code 1 and does not call scanner or LLM retry path.
    - Test 3: When `context_auto_compact` is enabled and scanned context is oversized, prompt input is compacted deterministically before render.
  </behavior>
  <action>In `research()`, call the new provider healthcheck before preflight/scanner/adapter work (per locked decision). Implement context auto-compact behavior controlled by `config.context_auto_compact`: if context exceeds a deterministic size threshold, compact it with clear truncation markers while preserving high-signal headers. Add focused integration tests validating ordering, failure short-circuit, and compact behavior.</action>
  <verify>
    <automated>python -m pytest tests/test_cli_brief_research.py -k "healthcheck or compact" -q</automated>
  </verify>
  <done>Research command fails fast on provider readiness issues and applies config-driven context compaction before prompt rendering, with tests proving both behaviors.</done>
</task>

</tasks>

<verification>
Run focused phase checks:
- `python -m pytest tests/test_provider_health.py tests/test_cli_brief_research.py -k "healthcheck or compact" -q`
</verification>

<success_criteria>
- `research()` enforces `provider_healthcheck` before scanner and LLM calls.
- Failure path is deterministic, actionable, and no downstream work executes.
- `context_auto_compact` behavior is config-driven, deterministic, and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-action-immediate-harden-config-with-small-model-tool-permissions-confirm-default-recommended-models-vs-all-models-model-aliases-context-auto-compact-and-provider-healthcheck-before-research/01-02-SUMMARY.md`
</output>
