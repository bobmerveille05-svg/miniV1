---
phase: 01-foundation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - run.py
  - minilegion/__init__.py
  - minilegion/core/__init__.py
  - minilegion/core/exceptions.py
  - minilegion/core/file_io.py
  - minilegion/core/config.py
  - minilegion/core/state.py
  - minilegion/cli/__init__.py
  - minilegion/adapters/__init__.py
  - minilegion/prompts/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_exceptions.py
  - tests/test_file_io.py
  - tests/test_config.py
  - tests/test_state.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "State machine rejects invalid forward transitions (e.g., init -> design) with InvalidTransitionError"
    - "State machine allows backward transitions and clears downstream approvals"
    - "State machine allows forward-by-one transitions (init -> brief, brief -> research, etc.)"
    - "Atomic write creates the file with correct content and never leaves a partial file on error"
    - "Config loads from JSON file with sensible defaults for missing fields"
    - "Config per-role engine lookup falls back to default model when role not in engines dict"
    - "Exception hierarchy has distinct categories: state, config, validation, LLM, preflight, approval, file I/O"
    - "All tests pass with pytest"
  artifacts:
    - path: "minilegion/core/state.py"
      provides: "State machine with Stage enum, transition validation, approval clearing"
      exports: ["Stage", "StateMachine", "ProjectState", "STAGE_ORDER"]
    - path: "minilegion/core/config.py"
      provides: "Pydantic config model with defaults and per-role engine lookup"
      exports: ["MiniLegionConfig", "load_config"]
    - path: "minilegion/core/file_io.py"
      provides: "Atomic write utility"
      exports: ["write_atomic"]
    - path: "minilegion/core/exceptions.py"
      provides: "Exception hierarchy for all error categories"
      exports: ["MiniLegionError", "StateError", "InvalidTransitionError", "ConfigError", "ValidationError", "LLMError", "PreflightError", "ApprovalError", "FileIOError"]
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "[project]"
    - path: "run.py"
      provides: "CLI entry point"
      min_lines: 4
  key_links:
    - from: "minilegion/core/state.py"
      to: "minilegion/core/exceptions.py"
      via: "raises InvalidTransitionError on invalid transition"
      pattern: "raise InvalidTransitionError"
    - from: "minilegion/core/state.py"
      to: "minilegion/core/file_io.py"
      via: "uses write_atomic for STATE.json persistence"
      pattern: "write_atomic"
    - from: "minilegion/core/config.py"
      to: "minilegion/core/exceptions.py"
      via: "raises ConfigError on invalid config"
      pattern: "raise ConfigError"
---

<objective>
Create the foundational Python package structure, core modules (exceptions, atomic file I/O, config, state machine), and their unit tests.

Purpose: Establish the scaffolding every subsequent phase builds on — state management, config loading, safe file writes, and typed error handling.
Output: Working Python package with 4 core modules and passing unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-RESEARCH.md
@.planning/phases/01-foundation-cli/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold and core modules</name>
  <files>
    pyproject.toml,
    run.py,
    minilegion/__init__.py,
    minilegion/core/__init__.py,
    minilegion/core/exceptions.py,
    minilegion/core/file_io.py,
    minilegion/core/config.py,
    minilegion/core/state.py,
    minilegion/cli/__init__.py,
    minilegion/adapters/__init__.py,
    minilegion/prompts/__init__.py
  </files>
  <behavior>
    State Machine:
    - StateMachine("init").can_transition("brief") -> True (forward by one)
    - StateMachine("init").can_transition("design") -> False (skip forward)
    - StateMachine("design").can_transition("research") -> True (backward)
    - Backtracking from design to research clears research_approved, design_approved, plan_approved, execute_approved, review_approved
    - All 7 valid forward transitions work: init->brief, brief->research, research->design, design->plan, plan->execute, execute->review, review->archive
    - archive has no forward transition

    Config:
    - MiniLegionConfig() has defaults: provider="openai", model="gpt-4o", api_key_env="OPENAI_API_KEY", timeout=120, max_retries=2, engines={}
    - MiniLegionConfig(engines={"researcher": "gpt-4o-mini"}).get_engine("researcher") -> "gpt-4o-mini"
    - MiniLegionConfig(engines={"researcher": "gpt-4o-mini"}).get_engine("builder") -> "gpt-4o" (falls back to default)
    - load_config on nonexistent file returns defaults
    - load_config on valid JSON returns parsed config
    - load_config on invalid JSON raises ConfigError

    File I/O:
    - write_atomic(path, "content") creates file with "content"
    - write_atomic creates parent directories if missing
    - write_atomic to existing file replaces content atomically
    - If write fails mid-operation, original file is unchanged

    Exceptions:
    - MiniLegionError is base for all custom exceptions
    - StateError, ConfigError, ValidationError, LLMError, PreflightError, ApprovalError, FileIOError all inherit from MiniLegionError
    - InvalidTransitionError inherits from StateError
  </behavior>
  <action>
    1. Create `pyproject.toml` with:
       - `[project]` section: name="minilegion", version="0.1.0", requires-python=">=3.10"
       - dependencies: typer>=0.24.0, pydantic>=2.12.0
       - `[project.optional-dependencies]` dev = ["pytest>=8.0"]
       - `[tool.pytest.ini_options]` testpaths = ["tests"]

    2. Create `run.py` — minimal entry point:
       ```python
       """MiniLegion CLI entry point."""
       from minilegion.cli import app
       if __name__ == "__main__":
           app()
       ```

    3. Create package structure with `__init__.py` files:
       - `minilegion/__init__.py` — set `__version__ = "0.1.0"`
       - `minilegion/core/__init__.py` — empty
       - `minilegion/cli/__init__.py` — create Typer app with `no_args_is_help=True`, `@app.callback()` for `--verbose` global option. Import commands module (defer to Plan 02 — add comment placeholder).
       - `minilegion/adapters/__init__.py` — empty (Phase 3)
       - `minilegion/prompts/__init__.py` — empty (Phase 5)

    4. Create `minilegion/core/exceptions.py` — exception hierarchy per RESEARCH.md Pattern 5:
       - Base: `MiniLegionError(Exception)`
       - Categories: `StateError`, `ConfigError`, `ValidationError`, `LLMError`, `PreflightError`, `ApprovalError`, `FileIOError` — all inherit from `MiniLegionError`
       - Sub-category: `InvalidTransitionError(StateError)`

    5. Create `minilegion/core/file_io.py` — atomic write per RESEARCH.md Pattern 4:
       - `write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None`
       - Uses `tempfile.mkstemp(dir=parent)` + `os.fdopen` + `os.fsync` + `os.replace`
       - Creates parent dirs with `mkdir(parents=True, exist_ok=True)`
       - Cleans up temp file on any error

    6. Create `minilegion/core/config.py` — config model per RESEARCH.md Pattern 2:
       - `MiniLegionConfig(BaseModel)` with defaults: provider="openai", model="gpt-4o", api_key_env="OPENAI_API_KEY", timeout=120, max_retries=2, engines=dict
       - `get_engine(role: str) -> str` method — returns engines.get(role, self.model)
       - `load_config(project_dir: Path) -> MiniLegionConfig` — loads from `project-ai/minilegion.config.json`, returns defaults if file missing, raises ConfigError on invalid JSON

    7. Create `minilegion/core/state.py` — state machine per RESEARCH.md Pattern 3:
       - `Stage(str, Enum)` with 8 values: init, brief, research, design, plan, execute, review, archive
       - `STAGE_ORDER = list(Stage)`
       - `FORWARD_TRANSITIONS` dict mapping each stage to its next stage
       - `APPROVAL_KEYS` list of approval key strings
       - `ProjectState(BaseModel)` with current_stage, approvals dict, completed_tasks list, history list, metadata dict
       - `HistoryEntry(BaseModel)` with timestamp, action, details
       - `ProjectState.add_history(action, details)` method
       - `StateMachine` class:
         - `__init__(current_stage: Stage, approvals: dict[str, bool])`
         - `can_transition(target: Stage) -> bool` — forward-by-one or backward only
         - `transition(target: Stage) -> None` — validates transition, clears downstream approvals on backtrack, raises InvalidTransitionError on invalid
       - `save_state(state: ProjectState, path: Path) -> None` — serializes via `model_dump_json(indent=2)` and calls `write_atomic`
       - `load_state(path: Path) -> ProjectState` — reads file and validates via `ProjectState.model_validate_json()`
  </action>
  <verify>
    <automated>pip install -e ".[dev]" && python -c "from minilegion.core.state import Stage, StateMachine; from minilegion.core.config import MiniLegionConfig; from minilegion.core.file_io import write_atomic; from minilegion.core.exceptions import MiniLegionError; print('All imports OK')"</automated>
  </verify>
  <done>
    - pyproject.toml exists with correct dependencies
    - run.py exists and imports app
    - All package __init__.py files exist
    - exceptions.py has MiniLegionError base + 7 category subclasses + InvalidTransitionError
    - file_io.py has write_atomic function using tempfile + os.replace
    - config.py has MiniLegionConfig model with defaults + load_config function
    - state.py has Stage enum, StateMachine class with transition validation and approval clearing, ProjectState model, save_state/load_state functions
    - All modules importable
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core module unit tests</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/test_exceptions.py,
    tests/test_file_io.py,
    tests/test_config.py,
    tests/test_state.py
  </files>
  <behavior>
    Exceptions:
    - All exception classes are subclasses of MiniLegionError
    - InvalidTransitionError is subclass of StateError
    - Each exception can be raised and caught by category

    File I/O:
    - write_atomic creates file with correct content
    - write_atomic creates parent directories
    - write_atomic overwrites existing file content
    - write_atomic leaves original untouched if write fails (simulate by patching os.replace to raise)

    Config:
    - Default config has expected default values
    - get_engine returns per-role override when set
    - get_engine returns default model when role not in engines
    - load_config returns defaults when file missing
    - load_config parses valid JSON correctly
    - load_config raises ConfigError on malformed JSON

    State Machine:
    - All 7 forward transitions are valid
    - Forward skip (init -> design) is rejected
    - Forward skip (init -> archive) is rejected
    - Same-stage transition (init -> init) is rejected
    - Backward transition (design -> research) is valid
    - Backward transition (archive -> init) is valid
    - Backtrack clears downstream approvals (design -> research clears research_approved through review_approved)
    - Forward transition does not clear approvals
    - can_transition returns correct bool without side effects
    - Invalid transition raises InvalidTransitionError
    - ProjectState serialization round-trip preserves data
    - save_state + load_state round-trip works correctly
  </behavior>
  <action>
    1. Create `tests/__init__.py` — empty

    2. Create `tests/conftest.py` with shared fixtures:
       - `tmp_project_dir(tmp_path)` — creates a temp directory simulating a project root with `project-ai/` subdirectory
       - `sample_config_json()` — returns a valid minilegion.config.json string with custom values (provider="openai", model="gpt-4o-mini", engines={"researcher": "gpt-4o"})
       - `default_approvals()` — returns dict with all approval keys set to False
       - `all_approved()` — returns dict with all approval keys set to True

    3. Create `tests/test_exceptions.py`:
       - Test each exception is subclass of MiniLegionError
       - Test InvalidTransitionError is subclass of StateError
       - Test catching MiniLegionError catches all subcategories
       - Test each exception carries message correctly

    4. Create `tests/test_file_io.py`:
       - `test_write_atomic_creates_file` — write to new file, verify content
       - `test_write_atomic_creates_parent_dirs` — write to nested path, verify dirs created
       - `test_write_atomic_overwrites_existing` — write twice, verify second content
       - `test_write_atomic_no_partial_on_error` — mock os.replace to raise, verify original file unchanged and temp file cleaned up
       - `test_write_atomic_handles_unicode` — write unicode content, verify correct encoding

    5. Create `tests/test_config.py`:
       - `test_default_config` — MiniLegionConfig() has correct defaults
       - `test_get_engine_override` — returns per-role engine
       - `test_get_engine_fallback` — returns default model for unset roles
       - `test_load_config_missing_file` — returns defaults
       - `test_load_config_valid_json` — parses correctly with custom values
       - `test_load_config_partial_json` — missing fields get defaults
       - `test_load_config_invalid_json` — raises ConfigError

    6. Create `tests/test_state.py`:
       - `test_all_forward_transitions_valid` — parametrize over all 7 pairs
       - `test_forward_skip_rejected` — init -> design raises InvalidTransitionError
       - `test_backward_transition_valid` — design -> research allowed
       - `test_same_stage_rejected` — init -> init is invalid
       - `test_backtrack_clears_downstream_approvals` — verify approval clearing
       - `test_forward_does_not_clear_approvals` — approvals unchanged on forward move
       - `test_can_transition_no_side_effects` — calling can_transition doesn't modify state
       - `test_project_state_serialization_roundtrip` — model_dump_json + model_validate_json preserves data
       - `test_save_load_state_roundtrip` — save_state then load_state returns equivalent ProjectState
       - `test_add_history` — ProjectState.add_history adds entry with timestamp
  </action>
  <verify>
    <automated>pytest tests/test_exceptions.py tests/test_file_io.py tests/test_config.py tests/test_state.py -v</automated>
  </verify>
  <done>
    - tests/ directory exists with __init__.py, conftest.py, and 4 test files
    - All tests in test_exceptions.py pass
    - All tests in test_file_io.py pass
    - All tests in test_config.py pass
    - All tests in test_state.py pass
    - pytest tests/ -v shows all green
  </done>
</task>

</tasks>

<verification>
pytest tests/ -v
python -c "from minilegion.core.state import Stage, StateMachine, ProjectState, save_state, load_state; from minilegion.core.config import MiniLegionConfig, load_config; from minilegion.core.file_io import write_atomic; from minilegion.core.exceptions import MiniLegionError, StateError, InvalidTransitionError, ConfigError; print('All core modules verified')"
</verification>

<success_criteria>
- pyproject.toml has correct project metadata and dependencies
- Package structure matches nested layout: minilegion/{core,cli,adapters,prompts}/
- State machine enforces linear-with-backtrack transitions and clears downstream approvals
- Config model loads from JSON with sensible defaults and per-role engine lookup
- Atomic write utility uses tempfile + os.replace pattern
- Exception hierarchy provides distinct categories for all error types
- All unit tests pass: pytest tests/ -v shows green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-01-SUMMARY.md`
</output>
