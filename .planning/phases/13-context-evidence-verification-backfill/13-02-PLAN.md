---
phase: 13-context-evidence-verification-backfill
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/phases/02-context-adapters/02-VERIFICATION.md
  - README.md
autonomous: true
requirements:
  - CTX-02
  - CTX-03
  - CTX-04
  - CTX-05
  - CTX-06
  - CFG-09
must_haves:
  truths:
    - "Phase 2 has explicit requirement-ID verification evidence for all CTX/CFG obligations."
    - "A reviewer can trace each requirement to concrete tests and implementation paths without reading summaries."
    - "Context config defaults are documented for users and match runtime defaults."
  artifacts:
    - path: ".planning/phases/02-context-adapters/02-VERIFICATION.md"
      provides: "Canonical requirements table for CTX-01..CTX-06 and CFG-08..CFG-09"
      contains: "| Requirement |"
    - path: "README.md"
      provides: "User-facing documentation for context config defaults"
      contains: "lookahead_tasks"
  key_links:
    - from: ".planning/phases/02-context-adapters/02-VERIFICATION.md"
      to: "tests/test_context_assembler.py"
      via: "requirement evidence rows"
      pattern: "test_context_assembler"
    - from: "README.md"
      to: "minilegion/core/config.py"
      via: "documented default values"
      pattern: "max_injection_tokens"
---

<objective>
Backfill audit-grade verification artifacts for Phase 2 and close documentation gaps for context defaults.

Purpose: Convert previously orphaned CTX/CFG evidence into strict, requirement-ID traceability that milestone audits can consume.
Output: A complete `02-VERIFICATION.md` requirement matrix plus README config default documentation aligned with runtime behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/02-context-adapters/02-01-SUMMARY.md
@.planning/phases/02-context-adapters/02-02-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>task 1: create Phase 2 verification artifact with requirement-ID mapping</name>
  <files>.planning/phases/02-context-adapters/02-VERIFICATION.md</files>
  <action>Create `02-VERIFICATION.md` with a strict requirements coverage table containing CTX-01..CTX-06 and CFG-08..CFG-09 rows. Each row must include: requirement description, status, exact evidence (test node IDs and implementation file references), and command used to verify. Use deterministic evidence references (specific pytest node paths, not prose-only claims).</action>
  <verify>
    <automated>python -m pytest tests/test_context_assembler.py tests/test_init.py tests/test_config.py -q</automated>
  </verify>
  <done>Phase 2 verification doc exists and every required ID has an explicit row with code/test evidence.</done>
</task>

<task type="auto">
  <name>task 2: document context config defaults for CFG-09</name>
  <files>README.md</files>
  <action>Update the configuration section to document `context.max_injection_tokens`, `context.lookahead_tasks`, and `context.warn_threshold` defaults and semantics. Keep documentation consistent with `ContextConfig` defaults in code and clarify that omitting `context` yields identical behavior via defaults.</action>
  <verify>
    <automated>python -m pytest tests/test_config.py -q</automated>
  </verify>
  <done>README contains accurate, user-facing context default docs that match code and CFG-09 expectations.</done>
</task>

<task type="auto">
  <name>task 3: run phase backfill regression gate and finalize evidence references</name>
  <files>.planning/phases/02-context-adapters/02-VERIFICATION.md, README.md</files>
  <action>Run the phase-specific regression command, then update any stale test-count/runtime text in `02-VERIFICATION.md` so it reflects current passing output. Ensure CTX-06 evidence references ADR fields explicitly (`status`, `context`, `decision`, `consequences`, `rejected alternatives`, `success criterion`) and that all links remain valid after Plan 13-01 changes.</action>
  <verify>
    <automated>python -m pytest tests/test_context_assembler.py tests/test_config.py tests/test_init.py -q</automated>
  </verify>
  <done>Verification artifact is internally consistent, regression command passes, and audit can trace all listed requirements without ambiguity.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_context_assembler.py tests/test_config.py tests/test_init.py -q` and confirm `02-VERIFICATION.md` contains requirement rows for CTX-01..06 and CFG-08..09 with up-to-date evidence links.
</verification>

<success_criteria>
- `.planning/phases/02-context-adapters/02-VERIFICATION.md` exists and maps all eight Phase 13 requirement IDs to evidence.
- README documents all `context.*` defaults and omitted-field behavior.
- Backfill regression command is green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-context-evidence-verification-backfill/13-02-SUMMARY.md`
</output>
