---
phase: 13-context-evidence-verification-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minilegion/core/context_assembler.py
  - tests/test_context_assembler.py
autonomous: true
requirements:
  - CTX-01
  - CFG-08
must_haves:
  truths:
    - "`minilegion context <tool>` includes a deterministic compact lookahead section when PLAN.json exists."
    - "`context.lookahead_tasks` changes how many pending tasks are injected into context output."
    - "Context assembly still succeeds when PLAN.json is missing or partially populated."
  artifacts:
    - path: "minilegion/core/context_assembler.py"
      provides: "Compact plan extraction and lookahead section assembly"
      contains: "## Compact Plan"
    - path: "tests/test_context_assembler.py"
      provides: "Behavior coverage for lookahead task count and graceful fallback"
      contains: "lookahead"
  key_links:
    - from: "minilegion/core/context_assembler.py"
      to: "project-ai/PLAN.json"
      via: "deterministic read + pending-task extraction"
      pattern: "PLAN.json"
    - from: "minilegion/core/context_assembler.py"
      to: "config.context.lookahead_tasks"
      via: "section item limit"
      pattern: "lookahead_tasks"
---

<objective>
Close the status->context completeness gap by wiring deterministic compact-plan lookahead into context assembly.

Purpose: Fulfill CTX-01 and CFG-08 runtime behavior (not just schema presence) with auditable, test-backed output.
Output: Updated assembler logic plus focused tests proving lookahead behavior and graceful degradation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-context-evidence-verification-backfill/13-RESEARCH.md
@minilegion/core/context_assembler.py
@tests/test_context_assembler.py

<interfaces>
From `minilegion/core/context_assembler.py`:
```python
def assemble_context(tool: str, project_dir: Path, config: MiniLegionConfig) -> str:
```

From `minilegion/core/config.py`:
```python
class ContextConfig(BaseModel):
    max_injection_tokens: int = 3000
    lookahead_tasks: int = 2
    warn_threshold: float = 0.7
```

From `minilegion/core/context_assembler.py` (existing output contract):
```text
## Current State
## Previous Artifact
## Stage Template
## Memory
## Adapter Instructions
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing tests for compact lookahead behavior</name>
  <files>tests/test_context_assembler.py</files>
  <behavior>
    - Test 1: When PLAN.json has pending tasks and lookahead_tasks=2, output includes exactly two pending task bullets in a compact-plan section.
    - Test 2: When lookahead_tasks is changed (for example to 1), output limit changes deterministically.
    - Test 3: Missing PLAN.json or malformed/pattern-missing plan data does not crash and falls back to a stable "no plan context" message.
  </behavior>
  <action>Add targeted RED tests under a new test class for compact-plan behavior. Build fixtures for STATE.json + PLAN.json with completed task IDs so pending tasks can be derived deterministically. Keep assertions section-based (`## Compact Plan`) and avoid brittle full-string snapshots.</action>
  <verify>
    <automated>python -m pytest tests/test_context_assembler.py -q</automated>
  </verify>
  <done>New tests fail before implementation and clearly encode lookahead count, pending-task selection, and graceful fallback expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement compact plan lookahead in assembler</name>
  <files>minilegion/core/context_assembler.py, tests/test_context_assembler.py</files>
  <behavior>
    - Test suite from task 1 passes with deterministic ordering and limits.
    - Existing context sections and graceful degradation behavior remain intact.
  </behavior>
  <action>Update `assemble_context()` to read `project-ai/PLAN.json` when present, derive pending tasks by excluding already completed task IDs from STATE, and inject a `## Compact Plan` section capped by `config.context.lookahead_tasks`. Keep the function pure, preserve existing warning/truncation behavior, and use stable fallback text when plan data is unavailable.</action>
  <verify>
    <automated>python -m pytest tests/test_context_assembler.py tests/test_config.py -q</automated>
  </verify>
  <done>`minilegion context` output now includes compact lookahead content controlled by config, and all context/config tests pass.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_context_assembler.py tests/test_config.py -q` and confirm lookahead-specific tests are green with no regressions in prior context sections.
</verification>

<success_criteria>
- `assemble_context()` consumes `context.lookahead_tasks` at runtime.
- Compact plan section is deterministic and bounded.
- Missing/invalid PLAN.json does not raise errors.
- Targeted test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-context-evidence-verification-backfill/13-01-SUMMARY.md`
</output>
