---
phase: 14-history-foundation-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minilegion/core/history.py
  - minilegion/core/state.py
  - tests/test_history.py
  - tests/test_state.py
autonomous: true
requirements:
  - HST-02
  - HST-03
  - HST-05
must_haves:
  truths:
    - "Legacy projects with embedded STATE history auto-migrate on first state load without data loss."
    - "STATE.json persists current state only and never writes a history field."
    - "History events can be appended and read deterministically through a dedicated API."
  artifacts:
    - path: "minilegion/core/history.py"
      provides: "Canonical append_event/read_history helpers plus legacy migration helper(s)."
      exports: ["append_event", "read_history"]
    - path: "minilegion/core/state.py"
      provides: "Current-state-only save/load path and migration trigger."
      contains: "load_state"
    - path: "tests/test_history.py"
      provides: "Unit coverage for append ordering, read ordering, and migration idempotency."
    - path: "tests/test_state.py"
      provides: "Regression coverage that persisted STATE excludes history while runtime API remains compatible."
  key_links:
    - from: "minilegion/core/state.py"
      to: "minilegion/core/history.py"
      via: "load_state migration hook"
      pattern: "migrate.*history"
    - from: "minilegion/core/history.py"
      to: "project-ai/history/*.json"
      via: "atomic event writes"
      pattern: "write_atomic"
---

<objective>
Establish the history subsystem contract and state migration backbone so history is durable and append-only while STATE remains current-only.

Purpose: Unlock Phase 14 integration without breaking existing projects that still carry embedded state history.
Output: `core/history.py` APIs, `state.py` migration/save refactor, and focused tests proving deterministic append/read + idempotent migration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-history-foundation-migration/14-CONTEXT.md
@.planning/phases/14-history-foundation-migration/14-RESEARCH.md
@minilegion/core/state.py
@minilegion/core/file_io.py
@tests/test_state.py

<interfaces>
From `minilegion/core/state.py`:
```python
class ProjectState(BaseModel):
    current_stage: str = "init"
    approvals: dict[str, bool] = Field(default_factory=...)
    completed_tasks: list[str] = Field(default_factory=list)
    history: list[HistoryEntry] = Field(default_factory=list)
    metadata: dict[str, str] = Field(default_factory=dict)

    def add_history(self, action: str, details: str = "") -> None: ...

def save_state(state: ProjectState, path: Path) -> None: ...
def load_state(path: Path) -> ProjectState: ...
```

From `minilegion/core/file_io.py`:
```python
def write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing coverage for history append/read and migration semantics</name>
  <files>tests/test_history.py, tests/test_state.py</files>
  <behavior>
    - Test 1: `append_event()` writes `NNN_<event_type>.json` with required fields and monotonic numbering.
    - Test 2: `read_history()` returns events sorted by numeric prefix, not filesystem order.
    - Test 3: loading legacy STATE payload with embedded `history` migrates events to `project-ai/history/` and strips `history` from persisted STATE.
    - Test 4: reloading already-migrated STATE does not duplicate events (idempotent migration).
    - Test 5: `ProjectState.add_history()` remains callable as compatibility shim while saved STATE excludes history.
  </behavior>
  <action>Create `tests/test_history.py` with focused append/read/migration test classes and extend `tests/test_state.py` with persistence assertions for current-only STATE. Keep tests deterministic by controlling timestamps and explicit fixture file order.</action>
  <verify>
    <automated>python -m pytest tests/test_history.py::TestAppendEvent tests/test_history.py::TestReadHistory tests/test_history.py::TestMigration tests/test_state.py::TestProjectState -q</automated>
  </verify>
  <done>Coverage exists for HST-03/HST-05 contracts and fails before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement history API module and state migration integration</name>
  <files>minilegion/core/history.py, minilegion/core/state.py</files>
  <behavior>
    - Test 1: append operations produce append-only indexed files under `project-ai/history/` with canonical JSON fields.
    - Test 2: read operations return typed, chronologically ordered events.
    - Test 3: legacy embedded history is migrated exactly once on first `load_state()` and STATE is rewritten without `history`.
  </behavior>
  <action>Implement `HistoryEvent` model and `append_event(project_dir, event)`/`read_history(project_dir)` in `minilegion/core/history.py` using `write_atomic()` and filename format `NNN_<event_type>.json`. Sanitize only filename suffix to safe lowercase underscore form while keeping original `event_type` in payload. In `state.py`, trigger migration inside `load_state()` when raw JSON contains `history`, persist migrated events to history files, and rewrite STATE without embedded history. Update `save_state()` to serialize current-state-only data (exclude `history`) and keep `ProjectState.add_history()` as runtime compatibility shim.</action>
  <verify>
    <automated>python -m pytest tests/test_history.py tests/test_state.py -q</automated>
  </verify>
  <done>`append_event()`/`read_history()` are exported and tested, `STATE.json` persistence is history-free, and legacy migration is non-destructive + idempotent.</done>
</task>

</tasks>

<verification>
Run focused contract suite:
- `python -m pytest tests/test_history.py tests/test_state.py -q`
</verification>

<success_criteria>
- Legacy STATE payloads with embedded history migrate automatically on first load and preserve chronology in `project-ai/history/`.
- Persisted `STATE.json` contains no `history` field after save/load cycles.
- History API provides deterministic append/read behavior for downstream command wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/14-history-foundation-migration/14-01-SUMMARY.md`
</output>
