---
phase: 14-history-foundation-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - minilegion/cli/commands.py
  - minilegion/core/context_assembler.py
  - tests/test_cli.py
  - tests/test_context_assembler.py
  - tests/test_init.py
autonomous: true
requirements:
  - HST-01
  - HST-04
must_haves:
  truths:
    - "Pipeline operations emit append-only history event files in `project-ai/history/` with sequential numbering."
    - "`minilegion history` prints a readable chronological timeline and graceful empty fallback."
    - "Context/status surfaces read recent events from history files, not embedded state history."
  artifacts:
    - path: "minilegion/cli/commands.py"
      provides: "CLI timeline command plus event append wiring for lifecycle commands."
      contains: "def history"
    - path: "minilegion/core/context_assembler.py"
      provides: "Recent history rendering sourced from `read_history()`."
      contains: "Recent history"
    - path: "tests/test_cli.py"
      provides: "Command registration and timeline output coverage."
    - path: "tests/test_context_assembler.py"
      provides: "Assembler regression proving history source moved to history events."
    - path: "tests/test_init.py"
      provides: "Init-path assertion that first operation yields history file(s)."
  key_links:
    - from: "minilegion/cli/commands.py"
      to: "minilegion/core/history.py"
      via: "append_event/read_history imports"
      pattern: "append_event|read_history"
    - from: "minilegion/core/context_assembler.py"
      to: "project-ai/history"
      via: "read_history call"
      pattern: "read_history"
    - from: "minilegion/cli/commands.py"
      to: "typer app"
      via: "@app.command history registration"
      pattern: "def history"
---

<objective>
Wire history as the operational source of truth across CLI and context surfaces, and expose user-facing timeline output.

Purpose: Complete the Phase 14 user-facing behavior so event history is visible, durable, and no longer coupled to STATE internals.
Output: lifecycle command event emission, `minilegion history` timeline command, and regression tests for CLI/init/context history behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-history-foundation-migration/14-CONTEXT.md
@.planning/phases/14-history-foundation-migration/14-RESEARCH.md
@.planning/phases/14-history-foundation-migration/14-01-SUMMARY.md
@minilegion/cli/commands.py
@minilegion/core/context_assembler.py
@tests/test_cli.py
@tests/test_context_assembler.py
@tests/test_init.py

<interfaces>
From `minilegion/core/history.py` (created in 14-01):
```python
def append_event(project_dir: Path, event: HistoryEvent) -> Path: ...
def read_history(project_dir: Path) -> list[HistoryEvent]: ...
```

From `minilegion/cli/commands.py`:
```python
@app.command()
def init(name: str) -> None: ...

@app.command()
def status() -> None: ...

@app.command()
def context(tool: str) -> None: ...
```

From `minilegion/core/context_assembler.py`:
```python
def assemble_context(tool: str, project_dir: Path, config: MiniLegionConfig) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add failing CLI/init/context tests for history event flow</name>
  <files>tests/test_cli.py, tests/test_context_assembler.py, tests/test_init.py</files>
  <behavior>
    - Test 1: `--help` output includes `history` command.
    - Test 2: `minilegion history` prints chronological entries and `_No history yet._` when no events exist.
    - Test 3: `init` path results in history event file(s) under `project-ai/history/`.
    - Test 4: `assemble_context()` recent-history block reads from history files (works when STATE has no embedded history).
  </behavior>
  <action>Extend existing test files with targeted assertions for the new command and history-backed read paths. Reuse `CliRunner` fixtures and keep timeline assertions stable by checking ordered event labels rather than raw timestamps.</action>
  <verify>
    <automated>python -m pytest tests/test_cli.py tests/test_context_assembler.py tests/test_init.py -q</automated>
  </verify>
  <done>All required history integration behaviors are covered and failing before command wiring changes.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire lifecycle commands and timeline CLI to history API</name>
  <files>minilegion/cli/commands.py</files>
  <behavior>
    - Test 1: lifecycle writes produce sequential history event files with canonical fields.
    - Test 2: `history` command outputs one-line chronological timeline entries.
    - Test 3: empty history directory yields `_No history yet._` without errors.
  </behavior>
  <action>Import history helpers and replace durable `state.add_history(...)` persistence with `append_event(...)` at lifecycle points (`init`, `brief`, `research`, `design`, `plan`, `execute`, `review`, `archive`, and redesign backtrack path). Add `@app.command()` `history` implementation that reads via `read_history()`, defaults to the newest 10 events, and prints chronological one-line rows (`timestamp event_type stage notes`). Keep graceful, non-fatal UX and existing `MiniLegionError` handling conventions.</action>
  <verify>
    <automated>python -m pytest tests/test_cli.py -q</automated>
  </verify>
  <done>CLI exposes `minilegion history`, operational commands emit append-only event files, and empty/no-event projects show `_No history yet._`.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: switch context recent-history rendering to read_history</name>
  <files>minilegion/core/context_assembler.py, tests/test_context_assembler.py</files>
  <behavior>
    - Test 1: recent history in context block reflects last events from history files.
    - Test 2: context history fallback remains `_No history yet._` when event log is empty.
  </behavior>
  <action>Replace `state.history[-3:]` usage in `assemble_context()` with `read_history(project_dir)` and render the last three events in chronological order. Preserve existing section headings and graceful degradation semantics introduced in earlier phases.</action>
  <verify>
    <automated>python -m pytest tests/test_context_assembler.py tests/test_cli.py tests/test_init.py -q</automated>
  </verify>
  <done>Context and CLI history displays are both sourced from `project-ai/history/` and pass integration tests.</done>
</task>

</tasks>

<verification>
Run integration gate for phase-facing behavior:
- `python -m pytest tests/test_history.py tests/test_state.py tests/test_context_assembler.py tests/test_cli.py tests/test_init.py -q`
</verification>

<success_criteria>
- `project-ai/history/` receives sequential JSON events for lifecycle operations.
- `minilegion history` returns readable chronological output and graceful empty fallback.
- Context assembler and status-related history output paths no longer depend on embedded `STATE.history`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-history-foundation-migration/14-02-SUMMARY.md`
</output>
